import { HightlightBestDirective } from './hightlight-best.directive';

describe('HightlightBestDirective', () => {
  it('should create an instance', () => {
    const directive = new HightlightBestDirective();
    expect(directive).toBeTruthy();
  });
});
